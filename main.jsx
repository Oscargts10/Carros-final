import React from "react";
import ReactDOM from "react-dom/client";
import VehiculosApp from "./VehiculosApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VehiculosApp />
  </React.StrictMode>
);